#include <stdint.h>
/* Prend en entré 8 bits et renvoie l'entier associé aux 4 bits de poids fort*/
//public uint_8 read_high(uint_8 symbol);
    //return symbol & 240;
/*Idem que read_high mais avec les bits de poids faible*/
//public uint_8 read_low(uint_8 symbol);
//    return symbol & 15;

/*Lis m bit sur le stream et renvoie l'entier (l'indice) associé*/
//public int get_indice(uint_8 m, FILE *stream);
/*Lis les 63 coefficients AC et les mets dans le tableau en parametre
    Renvoie 0 si tout se passe bien, renvoie autre chose sinon (1 ou -1)
*/
//public int lecture_AC(int *AC);
